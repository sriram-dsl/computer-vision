## Laerning

> It’s not a spelling mistake—the model failed to predict the context vector correctly.  
> We should focus on helping the model learn the right context representation;  
> once it does, the word prediction will update automatically.
